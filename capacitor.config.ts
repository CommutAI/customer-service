import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.customerservice',
  appName: 'customer-service',
  webDir: 'dist'
};

export default config;
